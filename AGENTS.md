# AGENTS.md

- 本 repo 是 AntiGravity 2 懶人包；技能位於 `.agents/skills/`。
- 使用繁體中文與 PowerShell。
- 平台專用設定必須留在平台適配層，不寫入共用核心流程。
- 不提交憑證、token、Cookie、本機私有路徑或學生個資。
- 修改後檢查十五個 `SKILL.md` frontmatter，並以 `agy` sandbox 唯讀驗證。
